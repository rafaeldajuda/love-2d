function loadActor(mode, x, y, height, width, type) 
    local actor = {
        mode = mode,
        x = x, 
        y = y,
        height = height,
        width = width,
        type = type
    }
    return actor
end